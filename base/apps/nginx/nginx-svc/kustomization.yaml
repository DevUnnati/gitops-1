## Autogenerated by Git management UI
###############################################################################################
## Kustomization for POD: nginx-svc
###############################################################################################
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: nginx-svc-conf
    files:
      - default.conf=./config/default.conf

generatorOptions:
  disableNameSuffixHash: true

  