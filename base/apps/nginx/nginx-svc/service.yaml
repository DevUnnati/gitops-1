## Autogenerated by Git management UI
###############################################################################################
## Service for POD: user-svc
###############################################################################################
apiVersion: v1
kind: Service
metadata:
  name: nginx-svc-http # name to be referred in ingress file in overlay
  labels:
    app-group: nginx-svc
    module-group: nginx
    product-group: gitops
spec:
  selector:
    app-group: nginx-svc
  type: ClusterIP
  ports:
    ## Service Ports for user-svc
    - name: http #Port Name to be used in ingress file in overlay
      port: 8080
      protocol: TCP
      targetPort: 8080 #keep both targetPort and Port same for type ClusterIP as above. This port number should match the container port in deployment.yaml